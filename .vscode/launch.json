{
    "version": "0.2.0",
    "configurations": [
      {
        "type": "node",
        "request": "launch",
        "name": "Запуск Node.js",
        "program": "${file}",
        "cwd": "${workspaceFolder}"
      }
    ]
  }